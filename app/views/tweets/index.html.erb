<p id="notice"><%= notice %></p>

<div class="py-10 text-2xl font-bold">Tweets</div>
<div class="flex flex-col space-y-3">
    <% @tweets.each do |tweet| %>
      <%= render(TweetComponent.new(tweet: tweet))%>
    <% end %>
</div>
<br>

<%= link_to 'New Tweet', new_tweet_path %>
